# Point the dashboard at the API. Default (no value) → the live Render API.
VITE_API_URL=https://orakon-trip-api.onrender.com
# For local dev against a local API, use instead:
# VITE_API_URL=http://localhost:4000
VITE_TRIP_ID=demo-trip
